 # 格式化字符串基础知识
+ 64位与32位的区别在于参数的传递，64位前6个参数**由寄存器传递**，因此计算偏移时找到栈上偏移后**要加5（6-1）**

+ 做这类题时一般先用gdb并在printf处下断点并用x命令查看栈上数据，或者用aaaa%p.%p.%p来确定偏移

+ 需要注意的是用来泄露数据时要注意%p和%s的区别

# 泄露数据（任意地址）
+ 需要先找到输入函数对应缓冲区的偏移，假设偏移为x,需要泄露的地址为AAAA，
则payload= **p32(AAAA)+b'%x$s'**,也可以通过在AAAA前加上一些垃圾字节**微调偏移量**